# Build output (generated by CMake; do not commit)
build/

# Local SQLite database (created at runtime; differs per machine)
weather.db

# Tool cache directory
.cache/

# macOS folder metadata (created automatically by Finder)
.DS_Store
